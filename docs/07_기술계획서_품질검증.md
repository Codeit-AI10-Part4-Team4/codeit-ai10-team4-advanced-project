# 기술계획서 — 광고 품질 검증 (AI 손님 패널)

> 담당 범위: **광고 품질 검증** — [05 최종계획서](05_최종계획서.md) 확장 지점
> 담당: 이아인(데이터·패널) · 이수호(평가·집계)
> 작성일 2026-08-06 · 상태: **팀 합의 대기** ([06 기획서 §6](06_AI손님패널_기획.md))
> 배경·목표는 [06 기획서](06_AI손님패널_기획.md) 참조

> 📌 **이 문서를 읽는 법**
> §5.1이 기본 파이프라인과의 **계약**입니다. 여기만 합의되면 나머지는 우리 내부 구현입니다.
> 반대·보완 의견은 이 문서에 직접 달아주세요.
> ⚠️ 가정: 백엔드는 레포 규약대로 Python/FastAPI(`src/api` thin 라우터 + `src/app_core` 도메인), 평가 LLM은 API 호출. 이미지 생성은 기본 파이프라인 담당이라 범위 밖.

---

## 1. Requirements (요구사항 + 수용 기준)

| # | 요구사항 | 수용 기준 (이게 되면 완료) |
|---|---|---|
| R1 | 주소+업종 → 상권 피처 기반 패널 자동 구성, 비중 수동 조정 | 서울 임의 주소로 10초 내 페르소나 10~20개 + 가중치 표시. 슬라이더 조정 → 재평가에 반영 |
| R2 | **기본 파이프라인 출력을 평가 입력으로 수용** | `evaluate(image, headline, sub, items, context)` 호출 한 번으로 평가 완료. 넘겨받은 필드만으로 동작(추가 입력을 요구하지 않음) |
| R3 | 미지원 입력 방어 | 서울 외 주소·미지원 업종·주소 누락 시 명시적 에러 코드 + 사장님 언어 안내. 크래시 없음 |
| R4 | 패널 평가: 유형 × 가중 집계, 시안당 평가 콜 ≤20, **30초 이내** | 평가 1회당 호출 수·소요 시간이 로그로 확인 가능하고 각각 20 이하 / 30초 이하 |
| R5 | 평가 신뢰성: 스키마 강제 · 근거 필수 · 근거↔피처 대조 · 분산 체크 | 대조 불일치 응답이 집계에서 제외됨을 로그로 확인. 분산 초과 시 결과 화면에 "신뢰도 낮음" 배지 |
| R6 | 결과 화면: 지표 3종 + 저항 요인 + 개선 제안 + 페르소나 코멘트 | 전문용어 없이 렌더. 각 지표에 근거 상권 데이터 툴팁 |
| R7 | 재생성 연결: 개선 제안을 기본 파이프라인에 전달 | "제안 반영해 다시 만들기" 클릭 → `suggestions[]`·`top_resistance[]`가 재생성 입력으로 전달됨 |
| R8 | 선택 로깅 | 평가·재생성·채택 이벤트가 event_log 테이블에 적재 |
| R9 | 평가 횟수 제한 + 사용량 로깅 | 세션당 평가 10회 초과 시 안내 메시지(값은 환경변수). 생성 한도는 기본 파이프라인 소관 |
| R10 | 모바일 웹 우선 | 375px 뷰포트에서 평가 관련 화면 사용 가능 |

## 2. Features (기능 → 서브태스크)

| # | 기능 | 서브태스크 | 담당 | 일정 |
|---|---|---|---|---|
| F0 | **스키마 확정** | `TradeAreaFeatures`·`Persona`·`EvaluationResult` Pydantic 확정 → `schemas.py` 커밋 | **수호**(아인은 요청) | **D1** |
| F1 | 데이터 엔진 | CSV 다운로드·검수 → ETL 스크립트(wide→long 변환, DuckDB 적재) → 카카오 주소→좌표→행정동 → 상권 매칭 → `build_features()` + 단위 테스트 | 아인 | D1–4 |
| F2 | 패널 빌더 | 축 값 유도 룰(§7.1) → 유형 세그먼트 코드 → LLM 서사 생성(배치 1콜) → 가중치 계산 → 수동 조정 API | 아인 | D5–7 |
| F5 | 평가 엔진 | 평가 프롬프트 + JSON 스키마 → 병렬 호출(asyncio) → 스키마·근거 검증 → 가중 집계·분산 체크 | 수호 | D2–7 |
| F6 | 결과 화면+연결 | 지표 바·저항 요인·제안·코멘트 렌더 → 재생성 CTA | 수호 | D8–10 |
| F7 | 사용량·로깅 | 세션 쿠키 → 평가 카운터 → event_log 적재 → 한도 미들웨어 | 수호 | D8–10 |
| F8 | 통합 | 기본 파이프라인과 계약 맞추기 → end-to-end 연결 → 미지원 케이스 방어 | 공동 | D11–13 |

- **F5는 F1을 기다리지 않는다**: F0에서 스키마를 고정하면 평가 쪽은 하드코딩 픽스처(`tests/fixtures/features_yeoksam_20261.json`)로 D2부터 시작한다. F1이 끝나면 픽스처를 실제 함수 호출로 교체만 하면 된다.
- **`schemas.py`는 수호 단독 소유**: 둘 다 쓰지만 **쓰는 사람은 한 명**이다. 아인은 직접 고치지 않고 필요한 변경을 요청한다(무엇이·왜 필요한지 + 실제 데이터 근거). 같은 브랜치에 둘이 푸시하므로, 공동 소유로 두면 충돌이 난다.

## 3. 시스템 아키텍처

```
[프론트] ──> [FastAPI 단일 서비스]
              ├─ src/app_core/generation/   ← 기본 파이프라인 (nlu·copy_gen·image_gen·layout·compliance)
              └─ src/app_core/panel/        ← 품질 검증 (features·panel_builder·evaluator)
                   ├─ DuckDB 파일 (상권 피처 + 평가 로그)
                   ├─ LLM API (패널 서사 1콜 + 평가 ≤20콜)
                   └─ Kakao Local API (주소→좌표→행정동)

호출 경계:  완성 광고물 + context ──> evaluate() ──> EvaluationResult ──(suggestions)──> 재생성
```

- 패널 모듈은 **기본 파이프라인 코드를 import하지 않는다.** 계약은 §5.1 함수 시그니처와 §6 스키마뿐이고, 반대 방향도 함수 하나로만 부른다.
- 상태는 **DuckDB + 세션 쿠키(UUID)**로만 관리. 서버 메모리 세션 없음.
- 배포·인프라(Docker Compose·nginx·GCP VM)는 **팀 공통** — 이 문서의 명세 대상 아님. GPU는 이미지 생성(SDXL)이 쓴다.
- 공유 파일 주의: `configs/*.yaml`·`flow.yaml`은 기본 파이프라인 소유. 업종 추가가 필요하면 PR 전에 공유.

## 4. 데이터 명세

### 4.1 소스 (전부 CSV 사전 다운로드, ETL 1회)

| 소스 | 데이터셋 | 쓰는 컬럼 | 용도 |
|---|---|---|---|
| 서울 열린데이터광장 | 상권분석서비스 **추정매출-상권** (분기) | 상권코드, 서비스업종코드, 성별/연령대/시간대/요일별 매출금액·건수 | 고객 구성 비중, 객단가 |
| 〃 | 상권분석서비스 **길단위인구(유동인구)-상권** | 상권코드, 시간대/성별/연령대별 유동인구 | 시간대 맥락, 패널 가중치 |
| 〃 | 상권분석서비스 **상권영역** | 상권코드, 상권명, 좌표(중심점·영역) | 주소→상권 매칭 |
| 〃 | 상권분석서비스 **점포-상권** | 상권코드, 업종, 점포수, 개폐업 | 경쟁 밀도 |
| 공공데이터포털(소진공) | **상가(상권)정보** 서울 | 상호, 업종분류, 행정동, 좌표 | 경쟁 점포 보조, 업종 매핑 |
| Kakao Local API | 주소 검색 + coord2regioncode | — | 주소→좌표→행정동 (실시간 호출, 무료 쿼터) |

- 최신 분기 1개만 적재 (이력 불필요). 원본 CSV는 `data/raw/`에 보존, 재적재 가능하게.
- 인코딩은 **cp949**. 상권 1,650개(골목 1,090 / 전통시장 305 / 발달 249 / 관광특구 6).

### 4.2 DuckDB 스키마

```sql
-- ETL 산출 (읽기 전용)
commercial_area(area_cd PK, area_nm, gu, dong_cd, center_lon, center_lat)
sales(area_cd, quarter, category_cd, gender, age_band, time_band, day_type, amount, cnt)  -- 원본 wide → long 변환
foot_traffic(area_cd, quarter, gender, age_band, time_band, population)
store_stat(area_cd, quarter, category_cd, store_cnt, open_cnt, close_cnt)

-- 런타임 기록
session_usage(session_id PK, eval_count, created_at)
event_log(id, ts, session_id, event_type, payload JSON)
  -- event_type: panel_built | evaluated | regen_requested | adopted
```

### 4.3 피처 함수 (F1의 산출물이자 패널 전체의 입력)

```python
def build_features(address: str, category: str) -> TradeAreaFeatures:
    # 1. Kakao: 주소 → (lon, lat) → 행정동코드
    # 2. commercial_area에서 최근접 상권 매칭 (중심점 거리, 상위 1개 + 반경 내 보조 상권)
    # 3. sales/foot_traffic/store_stat 최신 분기 조인 → 비중 계산
    # 4. TradeAreaFeatures 반환 (전 필드 결정적 — LLM 미개입)
```
- **지역 중립 인터페이스**: 시그니처와 반환 스키마에 서울 종속 없음. 경기 확장 시 어댑터만 추가.
- 매칭 실패(서울 외 주소, 상권 없음, 미지원 업종) → 명시적 에러 코드로 프론트에 안내 (R3).

### 4.4 CSV 실물 검수 결과 (2026-08-06 완료) — §6 스키마의 근거

**최신 분기 = `20261`(2026년 1분기).** 추정매출·점포는 20251~20261, 길단위인구는 2021Q1~2026Q1 수록.

**① 성별×연령 교차 데이터가 없다 (설계 영향 큼)**
원본은 `남성_매출_금액`/`여성_매출_금액`과 `연령대_30_매출_금액`이 **따로** 있고, "30대 여성 매출"은 존재하지 않는다.
→ `gender_share`·`age_share` 두 축으로 분리하고, 페르소나 `weight`는 **두 축의 곱**(독립 가정)으로 만든다.
→ `evidence`는 곱한 값이 아니라 **원본 두 값을 각각 인용**하게 해서(`gender_share.F` + `age_share.30`) 근거 대조가 실제 데이터와 1:1로 맞게 한다. 독립 가정은 실제 상관을 무시하므로, 페르소나 가중치는 "정확한 인구수"가 아니라 **상대적 비중**으로만 쓴다.

**② 성별·연령 합계가 1이 아니다 (미상 매출)**
요식업·카페 6,573행 기준 합계 평균 **0.879**, 중앙값 0.918, 최소 0.065 (법인카드·비식별 결제 추정). 성별합과 연령합은 항상 일치.
→ 합계로 나눠 정규화하고, 그 합계를 `demo_coverage`로 보존한다. **0.5 미만이면(전체의 1.2%) 결과 화면에 신뢰도 낮음 표시.**

**③ 유동인구 ≠ 구매층 — 시간대는 매출 기준을 쓴다**
역삼역 커피-음료 기준, 유동인구 시간대는 평탄(최고 06-11의 0.215)한데 **매출은 11-14가 0.479로 압도적**이다. "지나다니는 사람"과 "사는 사람"이 다르다.
→ `time_share`는 **매출 시간대**로 만든다. 유동인구는 `foot_age_share`(연령 도달층)만 남겨 **경계 페르소나 근거**로 쓴다 — "지나다니지만 사지 않는 층"이 곧 경계 페르소나이므로 이 차이 자체가 근거가 된다.

**④ 좌표계가 WGS84가 아니다**
`엑스좌표_값` 182,509~215,352 / `와이좌표_값` 437,249~465,573 → 중부원점TM(EPSG:5181 계열). 카카오 API는 WGS84를 반환하므로 변환 필요(`pyproj`).
→ 완화책: 영역-상권에 `행정동_코드`가 있으므로 **카카오 `coord2regioncode`로 행정동을 받아 1차 필터 → 그 안에서만 거리 비교**하면 좌표 변환 오차의 영향이 줄어든다.

**⑤ 컬럼명 차이·오타**
- `점포_수` → 실제는 `전체_점포_수` (`일반_`/`프랜차이즈_`/`개업_`/`폐업_점포_수` 별도)
- 원본 오타: `시간대_건수~06_매출_건수` (`시간대_00~06_매출_건수`여야 함). 매출 **금액** 컬럼은 정상.

**⑥ 데이터 노후화 영향 실측 — 무시 가능**
같은 1분기끼리 비교한 연령·성별 비중의 평균 절대 변화(상권 1,649개):

| 비교 | 변화 폭 |
|---|---|
| 2025Q1 → 2026Q1 (1년) | 0.54~0.88%p |
| 2024Q1 → 2025Q1 (1년) | 0.53~0.84%p |
| **2021Q1 → 2026Q1 (5년)** | **1.11~2.02%p** |

상권의 인구 구성은 5년이 지나도 2%p 안쪽으로만 움직인다. 분기 지연(약 2분기)은 페르소나 가중치에 유의미한 영향을 주지 않는다. 단, 결과 화면에 **"2026년 1분기 서울시 상권분석 데이터 기준"**을 표기해 신선도를 오해하지 않게 한다.

**⑦ 실시간 데이터([OA-22385](https://data.seoul.go.kr/dataList/OA-22385/F/1/datasetView.do)) 교차검증 — 해봤고, 안 쓰기로 했다**

⑥의 결론을 다른 소스로 검증해보려고 서울시 실시간 도시데이터를 광화문·덕수궁에서 뽑아 인근 상권 4곳(광화문역·서촌·종로구청·종각역)의 2026Q1 유동인구와 비교했다(2026-08-06 17:20).

| 항목 | 분기(2026Q1) | 실시간 | 차이 |
|---|---|---|---|
| **성별 M/F** | 51.0 / 49.0 | 51.6 / 48.4 | **0.6%p** |
| 연령 6구간 | — | — | 평균 **3.63%p** |

- **성별은 0.6%p로 사실상 일치.** 통신사 생활인구와 길단위 유동인구라는 **독립적인 두 소스**가 같은 답을 냈다 — 데이터 자체의 신뢰성 근거로 쓸 수 있다.
- **연령 차이 3.63%p는 노후화가 아니다.** 30·40·50대가 모두 상승(+2.8~+4.7%p)하고 10대·60대+가 하락(−3.7·−5.7%p)하는 **규칙적인 방향**이라 평일 17시대 직장인 패턴으로 읽힌다.
- **그래서 노후화 근거로는 ⑥(시계열 비교)만 쓴다.** 실시간 비교는 변수가 넷(측정 시각·공간 경계·계절·모집단 정의) 섞여 있어 무엇을 증명하는지 불분명하다. ⑥은 같은 데이터 안에서 같은 분기끼리 비교해 변수가 시간 하나뿐이다.
- 부수 소득: 연령 구성이 시각에 따라 3~5%p 움직인다는 정황은 **`time_share`를 페르소나 축으로 둔 설계(§7.1)를 지지**한다.

**제품에는 넣지 않는다.** 커버리지가 서울 주요 82곳뿐(우리는 상권 1,650개), 과거 데이터 미보관, 장소당 1콜이라 평가 30초 예산에 외부 API 왕복이 얹힌다. 세 가지 모두 우리 파이프라인과 맞지 않는다.

### 4.5 업종 매핑 (확정)

서울시 서비스업종 62개 중 요식업·카페 **10개**만 지원한다.

| 코드 | 명 | 코드 | 명 |
|---|---|---|---|
| CS100001 | 한식음식점 | CS100006 | 패스트푸드점 |
| CS100002 | 중식음식점 | CS100007 | 치킨전문점 |
| CS100003 | 일식음식점 | CS100008 | 분식전문점 |
| CS100004 | 양식음식점 | CS100009 | 호프-간이주점 |
| CS100005 | 제과점 | CS100010 | 커피-음료 |

`industries.yaml`의 `cafe` → CS100005·CS100010, `restaurant` → CS100001~004·CS100006~009로 매핑. 그 외 업종(`academy` 등)은 `UNSUPPORTED_INDUSTRY`.

## 5. 인터페이스 명세

### 5.1 계약 — 기본 파이프라인과의 경계 ⭐

[05 최종계획서](05_최종계획서.md)의 제안 인터페이스를 그대로 받되, `context`에 **`store_address` 한 필드 추가**를 요청드립니다.

> ✅ [05 최종계획서 §7-5](05_최종계획서.md)에 `store_address`가 **이미 수집 항목으로 들어가 있습니다**(전단지·현수막용). 새로 받으실 필요 없이 `context`로 넘겨만 주시면 됩니다.

```python
def evaluate(
    image: PIL.Image,              # 레이아웃 합성까지 끝난 최종 PNG
    headline: str, sub: str,       # 문구
    items: list[dict],             # [{"name","price"}]
    context: EvalContext,          # 아래
) -> EvaluationResult:             # §6
```

```python
class EvalContext(BaseModel):
    industry: str                  # industries.yaml id — "cafe" 등
    format: str; style: str        # formats/styles.yaml id
    product: str
    mode: Literal["preserve","scene"]
    store_address: str | None      # ★ 추가 요청 — 챗봇 슬롯에 이미 있는 값
```

- `store_address`가 **없으면**: 서울 평균 패널로 폴백하되 결과 화면에 "동네 데이터 없이 평가함" 배지. 그라운딩이 빠지면 이 기능의 의미가 사라지므로 폴백은 방어용일 뿐이다.
- `industry`가 요식업·카페 매핑에 없으면 `UNSUPPORTED_INDUSTRY` 에러 → 평가 버튼 비활성 (합의 대기).
- **이미지는 현 단계에서 평가에 쓰지 않는다.** 비전 콜은 콜 수·비용·응답 시간을 모두 늘린다. 문구·가격·context만으로 평가하고, 시각 품질 평가는 여력이 남으면 비전 1콜 옵션으로 추가한다. (인자는 인터페이스 안정성을 위해 지금 받아둔다.)
- **`format`·영업시간을 안 쓰는 이유**는 [06 기획서 §6](06_AI손님패널_기획.md) 참조 — 채널별 도달 보정은 근거 데이터가 없어 계수를 지어내야 하므로 하지 않는다.

### 5.2 패널 모듈 API (FastAPI, `/api` 프리픽스)

| 메서드/경로 | 요청 | 응답 | 비고 |
|---|---|---|---|
| POST `/panel/build` | `{address, category}` | `{panel_id, features, personas[]}` | R1 |
| PATCH `/panel/{id}/weights` | `{persona_id: weight}[]` | 갱신된 personas | 합=1 정규화는 서버가 |
| POST `/evaluate` | `{panel_id, ad_id, headline, sub, items, context}` | `EvaluationResult` | R4·R5. 평가 한도 차감 |
| POST `/events` | `{event_type, payload}` | 204 | R8 |
| GET `/usage` | — | `{eval_used, eval_limit}` | R9 |

- 재생성은 **이 모듈의 API가 아니다.** 프론트가 `EvaluationResult.suggestions`를 들고 기본 파이프라인의 재생성 엔드포인트를 호출한다 (R7).
- 인증 없음. 세션 쿠키(UUID, httpOnly)로 사용량 식별.
- 에러 규약: `{error_code, user_message}` — user_message는 사장님 언어(전문용어 금지). 코드: `OUT_OF_SEOUL` / `NO_TRADE_AREA` / `UNSUPPORTED_INDUSTRY` / `EVAL_LIMIT_EXCEEDED`.

## 6. 데이터 계약 (Pydantic — 그대로 구현)

```python
class TradeAreaFeatures(BaseModel):
    area_cd: str; area_nm: str; area_type: str     # 골목/발달/전통시장/관광특구
    gu_nm: str; dong_nm: str; quarter: str         # "20261"
    category_cd: str; category_nm: str             # "CS100010" / "커피-음료"

    # ⚠️ 원본에 성별×연령 교차가 없다 (§4.4). 두 축을 따로 두고 페르소나에서 곱한다.
    gender_share: dict[str, float]                 # {"M":0.493,"F":0.507} 합=1 (미상 제외 정규화)
    age_share: dict[str, float]                    # {"20":0.165,...,"60":0.081} 합=1
    demo_coverage: float                           # 정규화 전 성별·연령 합계 (미상 제외율, 낮으면 신뢰도↓)

    time_share: dict[str, float]                   # "11-14" → 0.479 (시간대 **매출** 비중)
    foot_age_share: dict[str, float]               # 유동인구 연령 비중 — 도달층, 경계 페르소나 근거

    weekend_ratio: float                           # 주말 매출 비중
    avg_ticket: int                                # 객단가(원) = 당월_매출_금액/당월_매출_건수
    avg_ticket_pct: float                          # 서울 동일 업종 내 분위 (0~1) — price_sens 판정용
    competitor_cnt: int                            # 동일 업종 전체_점포_수

class Persona(BaseModel):
    persona_id: str
    demo: str                                      # "30대 여성"
    axes: dict[str, str]                           # {"price_sens":"mid","motive":"habitual","time":"weekday_lunch"}
    weight: float                                  # 인구 비중 기반, 패널 내 합=1
    is_boundary: bool                              # 경계 페르소나 여부
    narrative: str                                 # LLM 생성 1~2문장
    evidence: list[FeatureRef]                     # 이 유형의 근거 피처 (필수, §8 대조 대상)

class FeatureRef(BaseModel):
    path: str                                      # "age_share.30" / "time_share.11-14"
    value: float                                   # LLM이 인용한 값 — 실제값과 코드 대조

class AdContent(BaseModel):                        # 평가 대상 — 파이프라인 출력에서 조립
    headline: str; sub: str
    items: list[dict]                              # [{"name","price"}] 그대로
    context: EvalContext                           # §5.1

class PersonaEval(BaseModel):
    persona_id: str
    attention: int; message: int; intent: int      # 0~100
    resistance: Literal["price","message","visual","relevance","none"]
    resistance_detail: str
    comment: str                                   # 1줄, 사장님 언어
    evidence: list[FeatureRef]                     # 필수 — 없으면 스키마 검증 실패

class EvaluationResult(BaseModel):
    ad_id: str
    scores: dict[str, float]                       # 가중 평균 3종
    confidence: Literal["ok","low"]                # 분산 체크 결과
    top_resistance: list[str]
    suggestions: list[str]                         # 재생성 입력으로 그대로 전달
    persona_comments: list[PersonaEval]            # 검증 통과분만
    excluded_cnt: int                              # 근거 대조 탈락 수 (투명성)
```

## 7. LLM 파이프라인 명세

| 단계 | 콜 수 | 출력 강제 |
|---|---|---|
| 패널 서사 생성 | 패널당 1콜 (유형 전체 배치) | JSON mode → Persona[] 부분 필드 |
| 패널 평가 | 시안당 ≤20콜 (병렬) | JSON mode → PersonaEval |

- **패널 모듈이 쓰는 LLM 콜은 이 둘뿐이다.** 생성·추출 콜은 전부 기본 파이프라인 소관.
- 모델·단가는 구현 시점 재확인. temperature: 평가=0.3(일관성), 서사=0.8.

### 7.1 패널 빌더 — 축 값 유도 룰 (전부 코드, LLM 미개입)
- **인구축**: `gender_share` × `age_share` 곱 → 최대 10개 세그먼트(성별 2 × 연령 5). **비중 3% 미만은 인접 연령대로 병합** (역삼역 사례에서 10대 0.3% → 20대로 병합).
- **시간대 맥락**: `time_share`(매출) 상위 구간 → weekday_lunch / afternoon / evening / morning (weekend_ratio > 0.4면 weekend 유형 추가)
- **가격 민감도**: `avg_ticket_pct` → <0.25 low / 0.25~0.75 mid / >0.75 high (상권 객단가가 높을수록 그 동네 손님의 가격 저항이 낮다는 뜻)
- **방문 동기**: 주중·특정 시간대 집중이면 habitual, 주말·분산이면 exploratory
- 세그먼트 = 인구축 × 행동축. **유형 수 10~20 유지** — 상위 인구 세그먼트 1~2개만 행동축으로 분할한다. 데이터 근거 없는 세분화(환각 다양성) 금지.
- **경계 페르소나 1~2개 필수**: `foot_age_share` > `age_share`인 연령대 — **지나다니지만 사지 않는 층**이다(역삼역 60대: 유동 12.4% vs 매출 8.1%). 가중치는 실제 비중대로 작게 — 점수 기여가 아니라 저항 요인·코멘트 다양성 목적.
- LLM은 각 세그먼트의 narrative 1~2문장만 생성. axes·weight·evidence는 코드가 채운다.

### 7.2 평가 프롬프트 구조 (페르소나 1콜)
```
[system] 너는 아래 특성의 손님이다. 광고를 보고 솔직하게 평가하라.
         부정적 평가를 주저하지 마라. 반드시 JSON 스키마로만 응답.
         evidence에는 제공된 피처의 path와 값을 그대로 인용하라 (새 수치 창작 금지).
[user]   ## 나의 특성: {persona.demo, axes, narrative}
         ## 우리 동네 데이터: {TradeAreaFeatures 관련 필드만}
         ## 광고물: {AdContent}
         ## 평가 항목: attention/message/intent (0~100), resistance, comment
```

### 7.3 재생성 연결 (R7)
- 패널 모듈은 재생성 프롬프트를 만들지 않는다. `suggestions`(사장님 언어 2~3문장)와 `top_resistance`를 그대로 넘긴다.
- 우리 책임은 **"카피에 바로 반영 가능한 구체적 문장"으로 쓰는 것**까지 (예: "가격을 낮추기보다 '런치 세트 8,900원'처럼 묶음가로 제시"). 프롬프트에 어떻게 넣을지는 기본 파이프라인 설계.

## 8. 신뢰성 파이프라인 (R5 — 기술 코어, 의사코드)

```python
async def evaluate(ad, panel):
    evals = await gather(*[call_llm(p, ad) for p in panel.personas])   # 병렬 ≤20
    valid = []
    for p, e in zip(panel.personas, evals):
        if not schema_ok(e):                       # ① Pydantic 검증
            e = await retry_once(p, ad)
            if not schema_ok(e): exclude(p); continue
        if not evidence_match(e, panel.features):  # ② 근거 대조
            e = await retry_once(p, ad)
            if not evidence_match(e, panel.features): exclude(p); continue
        valid.append((p, e))
    scores = weighted_mean(valid)                  # ③ 가중 집계 (제외분은 가중치 재정규화)
    conf = "low" if weighted_std(valid) > SIGMA_MAX else "ok"   # ④ 분산 체크
    return EvaluationResult(..., excluded_cnt=len(panel)-len(valid))

def evidence_match(e, features) -> bool:
    # FeatureRef.path로 실제값 조회, 상대 오차 5% 이내(비중) / 정확 일치(정수)만 통과
```

- 원칙: **정량(세그먼트·가중치·집계·검증)은 전부 결정적 코드, LLM은 서사·평가 생성만.** 신뢰 판단의 근거는 모델의 자기보고가 아니라 코드 검증 결과다.
- 잔여 콜 예산은 유형 추가가 아니라 **동일 유형 반복 샘플링**에 쓴다 — 분산 체크의 통계 근거.
- SIGMA_MAX 초기값 20 (0~100 스케일), 운영하며 조정. 환경변수로.

## 9. 콜·비용 예산 (패널 모듈만 — 생성 비용은 별도)

| 작업 | 토큰/콜 (in/out) | 비용/회 추정 |
|---|---|---|
| 패널 구성 (서사 1콜) | 2k / 1.5k | ~$0.001 |
| 평가 1회 (≤20콜) | 1.5k / 0.3k × 20 | ~$0.01 |

- 평가 1,000회 ≈ $10 → 데모·시연에 충분. 초과 방어는 R9 한도(`EVAL_LIMIT`).
- 사용량은 event_log로 일 단위 집계 (SQL 1줄로 확인, 대시보드 없음).

## 10. 프론트 화면 명세 (모바일 375px 기준) — 패널 담당분만

| # | 화면 | 핵심 요소 | 요구사항 |
|---|---|---|---|
| S1 | 우리 동네 손님 미리보기 | 상위 페르소나 3개 카드 + 구성 차트 + 비중 조정 슬라이더. 챗봇이 주소를 받은 직후 노출 | R1 |
| S2 | 결과 | 지표 바 3종(+신뢰도 배지) → 주 저항 요인 → 개선 제안 → 페르소나 코멘트 목록(가중치순) → **"제안 반영해 다시 만들기"** | R5~R8 |

- 시안 화면의 **"패널 평가받기" 버튼**은 기본 파이프라인 화면에 붙는 진입점 — **배치 협의 필요**.
- 결과 지표엔 근거 툴팁("이 상권 30대 여성 매출 34% 기준"). 프론트 프레임워크는 팀 규약을 따른다.
- 별도 유저플로우 문서는 만들지 않는다 — 화면이 2개이고 분기가 없어 이 표로 충분하다. 결과 화면 와이어프레임 1장은 F6 착수 직전(D7)에 그린다.

## 11. 배포·운영

- **배포는 팀 공통** (Docker Compose·nginx·GCP VM) — 이 문서의 명세 대상 아님.
- **추가되는 환경변수**: `KAKAO_REST_KEY`, `EVAL_LIMIT=10`, `SIGMA_MAX=20`.
- **추가되는 파일**: DuckDB 파일 1개(`data/panel.duckdb`) — 일 1회 스냅샷이면 충분.
- **로그**: 평가 호출별 (모델, 토큰, 소요시간, 제외 수) 1줄 로깅.

## 12. 디렉토리 구조

```
src/app_core/panel/            # 패널 모듈 — 기본 파이프라인 코드 import 금지
  features.py                  # build_features()              — 아인
  panel_builder.py             # 세그먼트 룰 + 서사 + 가중치     — 아인
  evaluator.py                 # 평가 호출·검증·집계            — 수호
  schemas.py                   # §6 Pydantic — **수호 단독 소유**, 아인은 요청만
src/api/routers/panel.py       # thin 라우터                    — 각자 담당 엔드포인트
etl/load_csv.py                # CSV → DuckDB (1회 실행)        — 아인
tests/fixtures/features_yeoksam_20261.json   # 역삼역 실측 픽스처 (평가 쪽 선행 개발용)
tests/app_core/panel/          # src 구조 미러링 (AGENTS.md 규약)
data/raw/ data/panel.duckdb    # gitignore
```

- 브랜치: `feat/quality-review`. CI 게이트(`ruff check` / `ruff format` / `mypy` / `pytest`) 통과 필수.
- 테스트 범위: **결정적 로직만**(피처 함수, 세그먼트 룰, 근거 대조, 집계). LLM 출력 자체는 테스트하지 않는다 — 검증 게이트가 런타임 테스트 역할.

## 13. 미결

- **합의 6건** — [06 기획서 §6](06_AI손님패널_기획.md). 특히 `store_address` 추가와 미지원 업종 처리.
- **"패널 평가받기" 버튼 배치**: 기본 파이프라인 결과 화면의 어느 위치인지 협의 필요.
- **좌표 변환 방식**: `pyproj`로 EPSG:5181→WGS84 변환할지, 행정동 코드 필터로 우회할지 (§4.4④). F1 구현 시 실측 후 결정.
- ~~CSV 실물 검수~~ — 2026-08-06 완료, 결과는 §4.4. 스키마를 데이터 모양에 맞춰 개정했다.
- ~~업종 매핑표~~ — §4.5에서 확정.
- 지역 확장(서울+경기): 미결 유지. `build_features()` 지역 중립 인터페이스로 대비만.
- 이미지 시각 품질 평가(비전 1콜): 여력이 남으면 추가.
