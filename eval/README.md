# eval/

평가 자산(골든 데이터셋·지표) 공간입니다. 코드와 라이프사이클이 달라
`src/`가 아닌 별도 최상위 디렉토리로 분리합니다.

## 규칙

- **지표 함수는 순수 함수로**: (예측, 정답)만 받아 점수를 계산 — 구현이 바뀌어도
  재사용 가능하게 (`eval/metrics.py` 권장).
- **골든 데이터셋(`eval/golden_dataset/`)은 평가 기준 자체**: 신중히 변경하고
  이유를 커밋 메시지에 명시하세요.
- 골든셋 CSV는 `.gitignore`의 `!golden_dataset*.csv` 예외로 커밋 가능합니다
  (그 외 데이터 파일은 커밋 금지).
- **측정 결과 JSON은 예외로 커밋합니다.** 원본 데이터가 아니라 *주장의 근거*라
  그렇습니다. 실측값을 문서에 인용해놓고 그 숫자가 어디서 나왔는지 남기지 않으면
  나중에 검증할 방법이 없습니다. 크기가 커지면(수백 KB) 이 예외를 다시 논의합니다.

---

# 패널 지표 (`panel_metrics.py`)

**현재 버전 V2** · 최종 수정 2026-08-14 · 담당 이수호

AI 손님 패널을 재는 순수 함수 모음. 외부 의존성 없음.

- `pearson` / `spearman` / `mean_absolute_error` — 사람 평가와의 일치도
- `price_contradictions` — 걸림돌 라벨의 자기모순 건수

위 규칙의 `metrics.py` 권장 이름 대신 `panel_metrics.py`를 쓴다. 생성 파이프라인 쪽에도
별도 지표가 생길 예정이라 파트별로 파일을 나눈다.

## 쓰는 곳

팀원이 광고 시안을 블라인드로 5점 평가한 값을 정답으로 놓고, 패널 점수와의 상관을 본다.
**"이 평가를 믿어도 되나"에 대한 유일한 외부 근거**라 발표 자료에 그대로 들어간다.
라벨은 모이는 대로 `eval/golden_dataset/`에 둔다.

`price_contradictions` 는 **"가격이 괜찮다고 해놓고 걸림돌은 price"** 인 코멘트를
센다. 쏠림 자체는 결함이 아니다 — 정말 비싼 광고면 맞는 답이고, 광고에 가격 말고
구체적인 게 없으면 손님이 반응할 것도 가격뿐이다. 결함인 것은 이 모순이다.

## 알려진 제약

낱말 맞추기라 완벽하지 않다. **판정이 아니라 계기판으로 쓴다.** 첫 판은 낱말
줄기(`"적당"`)만 봐서 실측에서 5건을 잡았는데 **5건 다 오탐**이었다 —
`"가격이 적당한지 고민"`(판단 유보)과 `"가격이 적당하다"`(단정)를 못 갈랐다.
지금은 단정형 어미까지 본다. **낱말을 늘리기 전에 테스트의 실측 코멘트로
반드시 확인할 것.** 놓치는 쪽으로 기울여 두었다 — 잘못 울리는 계기판은
안 울리는 것보다 나쁘다.

## 변경 이력

| 버전 | 날짜 | 변경 | 이유 |
|---|---|---|---|
| **V1** | 2026-08-06 | `panel_metrics.py` 최초 추가 (pearson·spearman·mae) | 사람 평가 대조 실험을 라벨 수집 즉시 돌릴 수 있게 미리 준비 |
| **V2** | 2026-08-14 | `price_contradictions` 추가 · 오탐 수정 | 걸림돌 쏠림을 느낌으로 판단하다 여러 번 헛짚어서 세는 쪽으로 바꿈 |

---

# 재생성 A/B (`run_ab_panel.py` · `ab_panel.json`)

**현재 버전 V1** · 2026-08-12 · 담당 이아인

패널 제안을 반영해 다시 만든 광고가 **실제로 나아지는가**를 잰다. 루프가 도는 것은
확인했지만 낫게 만드는지는 확인한 적이 없었고, 그게 이 제품의 핵심 주장이다.

- **심판은 `contrast.py` 의 `fit`** — LLM 이 아니라 서울시 실측 수치로 계산하는
  결정적 함수라 자기가 만든 것을 자기가 칭찬할 수 없다. LLM 에게 "좋아졌니?"를
  되묻는 순환 평가를 피하는 것이 설계의 핵심이다.
- **개선과 회피를 가른다** — 시점 언급을 지워 감점을 피한 것도 점수는 오른다.
  `weakest()` 가 `fit is None` 인 항목을 빼기 때문이다. 채점된 항목 집합을 같이
  기록해 항목이 사라진 경우를 따로 센다.
- **대조군을 둔다** — 패널 제안 대신 `copy_gen.REVISION_OPTIONS` 의
  "아예 다른 느낌으로"로 재생성한다. 없으면 *재생성 자체*의 효과와 *패널*의 효과를
  구분할 수 없다.

`ab_panel.json` 은 광고 1건당 한 행이고, 필드는 이렇다.

```
store area category product price
before  {headline sub weak weak_kind scored}    ← scored 는 fit 이 붙은 항목만
panel   {같은 구조}      control {같은 구조}
suggestions top_resistance confidence excluded_cnt
verdict_panel verdict_control secs
```

## 실측 결과 (2026-08-12, 광고 9건 · 실제 호출 189회)

**패널 9건 전부 "변화 없음". 대조군도 동일.** 다만 이것은 *패널이 효과 없다*는 뜻이
아니라 **심판이 패널이 바꾸는 것을 못 본다**는 뜻이다. 축이 안 겹친다.

| 축 | 가동 여부 |
|---|---|
| `price` | `brief.price` 로만 계산 → `revised()` 가 슬롯을 안 바꾸므로 **구조적으로 고정** |
| `timing` | 문구에 시점 말이 있어야 채점 → 패널 제안이 시점을 건드린 적 **0/18** |
| `weekend` | 같음 → **1/18** (그마저 사장님이 `situation` 에 쓴 말) |

9건 중 6건은 채점된 항목이 `price` 하나뿐이었다. 즉 재생성이 무엇을 하든
숫자가 못 움직이는 상태였다.

원인은 `evaluator._summarize` 가 모델에게 주는 "사실" 블록이 **가격 두 줄뿐**이라는
것이다. 시간대·주말·경쟁 수치는 `contrast()` 가 `_summarize` 호출 7줄 앞에서 이미
만들어두는데 넘어가지 않는다. 최악 시간대(매출 0.0%)를 대놓고 말하는 문구를 손으로
넣어봐도 제안은 가격 얘기였고, 유일한 시점 언급은 *"아침 메뉴라는 점을 강조하며"* —
아침 매출이 0.0% 인 동네에서 **방향이 반대**였다.

## 실측 결과 (2026-08-18, V2 표본 · 광고 9건 · 실제 호출 189회)

V1 은 9건 중 6건이 천장이라 **못 잰 것**이었다. V2 는 전부 시점을 말하는 광고로
바꿔 **8/9 를 측정 가능하게** 만들고 다시 쟀다.

```
패널   변화 없음 6 · 천장 1 · 회피 2      개선 0건
대조   변화 없음 7 · 천장 1 · 회피 1      개선 0건
```

**개선 0 건이다. 대조군보다 낫다는 증거가 없다.** 이번엔 "못 잼"이 아니라
**측정된 음성**이라 V1 과 뜻이 다르다.

### 배관은 고쳐졌다

V1 의 원인(`_summarize` 사실 블록이 가격 두 줄뿐)은 #27 에서 해결됐다.
`contrast()` 결과가 이제 사실 블록에 들어간다. 효과가 보인다:

```
시점을 건드린 제안   V1  0/18   →   V2  7/18
```

### 그런데 방향이 반대다

7건 전부 **광고가 이미 말한 시간대를 강조하라**고 한다. 매출이 몰리는 쪽으로
옮기라고 하지 않는다.

```
아이스 아메리카노  퇴근길 0.31  → "저녁에도 즐길 수 있는" 강조   (점심이 48%)
김치찌개          점심   0.34  → "점심 특가" 강조              (저녁이 50%)
순살 반반         점심   0.03  → "점심 시간대 특별 할인"        (저녁이 41%)
```

`timing_note` 의 문장에는 **"가장 많이 팔리는 때는 점심(11~14시)로 48%입니다"**
가 이미 들어 있다. 모델이 그 문장을 받고도 옮기라고 하지 않고 강조하라고 한다.
사실을 준 것만으로는 부족하고, **어느 방향이 나은지**가 전달돼야 하는 것으로 보인다.

`fit` 값(0.31)은 사실 블록에 안 들어간다 — `_summarize` 가 `n.text` 만 넘긴다.
다음 실험은 그 격차를 문장에 넣어보는 것이다.

### 회피 2건

`후라이드 치킨` · `양념치킨 세트` 는 재생성이 **시점 언급을 지워** 점수가 1.0 이
됐다. `verdict()` 가 "개선"이 아니라 "회피"로 세는 그 경우다 — 이 계측이 없었으면
개선 2건으로 읽혔다.

## 알려진 제약

- **PR #8 머지 전에는 `run_ab_panel.py` 의 import 가 실패한다** (`schema.Feedback`·
  `AdBrief.revised()` 가 그 PR 에 있다). 측정은 그 브랜치에서 돌렸다.
  `eval/` 은 CI 범위 밖이라 검사를 깨지는 않는다.
- 표본 9건. 비율이 아니라 건수로 읽어야 한다.
- 좌표를 박아 지오코딩을 건너뛰는데, "수유동" 좌표가 실제로는 **도봉산입구** 상권에
  매칭된다. A/B 는 같은 상권을 전후로 쓰므로 결과에 영향은 없지만 가게 이름이 오해를 준다.

## 변경 이력

| 버전 | 날짜 | 변경 | 이유 |
|---|---|---|---|
| **V1** | 2026-08-12 | A/B 스크립트·실측 결과 최초 추가 | "재생성이 광고를 낫게 만드는가"를 비워둘 수 없어서. 결과는 음성이었고, 그 과정에서 `_summarize` 의 사실 블록 문제를 찾았다 |
| **V2** | 2026-08-18 | 표본을 전부 시점 언급 광고로 교체 · 재측정 | V1 은 9건 중 6건이 천장이라 못 잰 것이었다. 8/9 를 측정 가능하게 만들어 다시 재니 개선 0건 — 이번엔 측정된 음성이다 |

---

# 이미지 골든셋 (`run_golden_images.py` · `run_photo_router.py`)

**현재 버전 V1** · 2026-08-14 · 담당 고귀한

이미지 생성 품질을 잴 고정 시험지. 5업종(분식한식·카페·베이커리·꽃집·소매) ×
3조건(밝음·어두움·복잡) = 15장으로, 사장님이 실제 올릴 법한 어려운 사진만 골랐다
(투명 유리컵, 갈색 위 갈색 빵, 얇은 금목걸이, 물건이 잔뜩인 매대 등).

- **사진은 커밋하지 않는다** — `data/golden_photos/선정/`(gitignore)에 두고,
  Pexels 원본 파일명을 `출처.txt` 로 남긴다. 필요하면 그 이름으로 재다운로드한다.
- **주문·문구는 상수로 고정** — 모델·처리 방식을 바꿀 때 그 차이만 보이게 하는
  조건 통제다 (`RECIPES`).

## 쓰는 법

    python eval/run_golden_images.py          # 15장 전부 → data/golden_results/ + 한판 2장
    python eval/run_golden_images.py 꽃집     # 한 업종만
    python eval/run_photo_router.py           # 사진 갈래 판정(photo_router) 채점 — 비전 15콜

## 실측 결과 (2026-08-13, 기준선)

15장 중 합격 6장. 문제 유형 5가지 — 그림자 없음 · 딸려온 조각 · 다중 객체 흩어짐 ·
누끼 빈손 · 크기 부조화. **10장은 모델이 아니라 처리 경로 문제였다** (원본을 살렸어야
할 사진을 분해). 이 측정이 photo_router(살릴지/오릴지/새로 그릴지 판정)의 근거다.

갈래 정답지는 `run_photo_router.py` 의 `LABELS` — 기준선 한판을 보고 사람이 정했다.
1층(마스크 규칙) 단독 채점에서 조각 수 신호는 죽어 있었다(흩어진 물건도 마스크에선
한 덩어리로 나온다). 흩어짐 판단은 2층(비전)으로 넘겼고, 2층 채점은 크레딧 복구 후 예정.

## 알려진 제약

- 사진이 로컬에만 있어 다른 팀원이 재현하려면 `출처.txt` 기준으로 같은 사진을 받아야 한다.
- 소매 업종의 주문(RECIPES)이 "동네마켓"으로 고정이라 화장품·목걸이 사진과 업종이
  안 맞는다 — 배경 프롬프트가 어긋나는 원인이므로 V2에서 사진별 업종 분리 예정.
- `run_golden_images.py` 는 [ml] extra + GPU, 포스터형은 OpenAI 키가 필요하다.

## 변경 이력

| 버전 | 날짜 | 변경 | 이유 |
|---|---|---|---|
| **V1** | 2026-08-14 | 골든셋 15장 선정·일괄 생성·갈래 채점 스크립트 | 개선을 감이 아니라 "몇 장 → 몇 장"으로 증명할 기준점이 필요해서 |

---

# 판별력 측정 (`accuracy.py`)

**현재 버전 V2** · 최종 수정 2026-08-13 · 담당 이수호

패널이 **좋은 광고와 나쁜 광고를 구분하는가**를 잰다.

정확도를 못 재면 개선을 말할 수 없다. 그런데 "실제 손님 반응"이라는 정답이
우리에게 없다. 그래서 정답 대신 **순서**를 쓴다 — 어느 쪽이 나은지 사람이면
이견 없이 답할 수 있는 광고 쌍을 만들어, 패널이 그 순서를 맞추는지 본다.

**판별력이 낮으면 점수는 노이즈다.** 쌍을 못 가르는데 절대 점수가 62.4 라고
말하는 것은 의미가 없다. 이 지표가 먼저다.

## 쌍은 실측에서만 만든다

역삼역 2026Q1 수치(점심 매출 47.9% · 객단가 9,546원 · 30대 38.2%)에서 벗어난
쪽이 "나쁜 광고"다. 취향으로 만든 쌍은 패널이 틀려도 **패널 탓인지 우리 탓인지
알 수 없다.**

| 쌍 | 갈래 | 무엇을 가르나 |
|---|---|---|
| 시간대 | 상권 | 매출이 몰리는 시간을 짚었는가 |
| 가격 | 상권 | 객단가에 맞는 가격인가 |
| 타깃 | 상권 | 이 동네 주 고객층에 말하는가 |
| 명료성 | 보편 | 무엇을 파는지 알 수 있는가 |

## 쓰는 법

```bash
MODEL_PROFILE=openai python eval/accuracy.py                    # 판별력 (라벨 불필요)
MODEL_PROFILE=openai python eval/accuracy.py --sheet            # 사람 평가용 시트
MODEL_PROFILE=openai python eval/accuracy.py --score labels.csv # 사람과 대조
MODEL_PROFILE=openai python eval/accuracy.py --ablate standing  # 그 요소가 값을 하는가
MODEL_PROFILE=openai python eval/accuracy.py --k 3              # 자기일관성 표본 수
```

`--sheet` 는 광고를 섞어 익명 키(`sha1[:8]`)로 내보낸다. 사람이 채점할 때
어느 쪽이 "좋은 쪽"인지 보이면 안 되기 때문이다 — 아인님 지적으로 추가했다.

`--ablate` 는 프롬프트 요소 하나를 빼고 같은 측정을 돌린다. **값을 못 하는
요소는 지운다는 전제**로 넣었다.

## 실측 결과

```
2026-08-11  standing 도입     상권 평균 점수차 +11.7 → +18~20 (3회 재현)
                             손님 편차 3.01 → 7.67 (--ablate standing)
                             판별 4/4 유지
재실행 흔들림                 0.1~3.1점 — 3점 미만 차이는 해석하지 말 것
```

## 알려진 제약

- 쌍이 4개뿐이고 상권 1곳(역삼)이다. 다른 상권·업종은 안 봤다.
- "사람이면 이견 없이 답한다"는 전제 자체를 검증하지 않았다. `--sheet`/`--score`
  가 그 검증용인데 라벨이 아직 없다.

## 변경 이력

| 버전 | 날짜 | 변경 | 이유 |
|---|---|---|---|
| **V1** | 2026-08-11 | 판별력 측정 최초 추가 | 12명 패널이 사실상 1명이었던 것을 이 도구로 처음 발견 |
| **V2** | 2026-08-13 | 블라인드 키 · `--ablate` · 지표별 점수차 | 시트에 정답이 노출됐고(아인님 발견), 프롬프트 요소의 값어치를 따로 재야 해서 |

---

# 패널 실측 (`run_panel_eval.py`)

**현재 버전 V2** · 최종 수정 2026-08-14 · 담당 이수호

실제 LLM 으로 패널 평가를 한 번 돌려 **탈락률·점수·걸림돌 분포**를 본다.
단위 테스트는 전부 `FakeClient` 라서, 모델이 규칙을 실제로 지키는지는 돌려봐야 안다.

DuckDB 를 안 쓴다 — 골든 픽스처를 그대로 읽으므로 ETL 없이 돌아간다.
**한 번에 14콜**(손님 12 + 걸림돌 분류 1 + 제안 요약 1).

## 쓰는 법

```bash
MODEL_PROFILE=openai python eval/run_panel_eval.py
MODEL_PROFILE=openai python eval/run_panel_eval.py --runs 3        # 재현성
MODEL_PROFILE=openai python eval/run_panel_eval.py --no-summary    # 요약 콜 끄기
MODEL_PROFILE=openai python eval/run_panel_eval.py --price 0       # 가격 없는 광고
MODEL_PROFILE=openai python eval/run_panel_eval.py \
  --product 아메리카노 --headline "점심 10분 컷, 아메리카노" --sub "..."
```

`--price 0` 은 "가격 없음"이다(`AdBrief.show_price`). **걸림돌이 입력에 반응하는지
재는 결정적 시험** — 적히지도 않은 가격을 걸림돌로 고르면 광고를 안 본 것이다.

문구와 상품이 어긋나면 경고를 띄운다. 어긋난 입력으로 잰 결과는 못 믿는다.

## 실측 결과 (2026-08-14)

걸림돌이 무엇을 보고 정해지는지를 6회에 걸쳐 좁혔다.

```
광고                avg_ticket  걸림돌 결정자   price
크로플 9,500원          줌       손님 콜        12/12
아메리카노 3,000원      줌       손님 콜        12/12
+ alternative 라벨      줌       손님 콜        12/12
가격 없음             안 줌      손님 콜         0/12
문구에만 9,500원      안 줌      손님 콜         0/12
아메리카노 3,000원      줌      분류 콜          1/12
```

마지막 줄은 앞줄과 **광고도 같고 준 숫자도 같다.** 누가 라벨을 고르느냐만 다르다.
근거로 인용할 수 있는 숫자가 라벨을 정하고 있었다.

## 알려진 제약

- 상권 1곳(역삼) · 광고 4건 · 각 1회다. 재현성은 `--runs` 로 따로 봐야 한다.
- 골든 픽스처가 12명 전원 `price_sens=mid` 라 가격 축은 아직 검증 불가.
  픽스처 재생성이 필요하다(A 파트).

## 변경 이력

| 버전 | 날짜 | 변경 | 이유 |
|---|---|---|---|
| **V1** | 2026-08-10 | 탈락률·재현성 측정 | 화면(F6) 착수 전에 모델이 근거 규칙을 지키는지 확인하려고 |
| **V2** | 2026-08-14 | 광고 갈아끼우기 · 걸림돌 모순 계측 | 브리프가 고정이라 "가격 없는 광고" 같은 결정적 시험을 못 돌렸다 |

---

# 걸림돌 라벨 회귀 (`run_label_check.py`)

**현재 버전 V1** · 최종 수정 2026-08-18 · 담당 이아인

나쁜 광고가 **왜** 나쁜지 제대로 짚는지 본다. `accuracy.py` 는 좋은 광고가
**높은 점수**를 받는지 보지만, 사장님 화면에 뜨는 것은 점수만이 아니라
**"무엇이 걸렸는지"** 다. `accuracy.py` 도 걸림돌 분포를 표로 찍기는 하는데
**합불을 가르지 않아서** 사람이 봐야 알고, 아무도 안 봤다.

광고를 새로 만들지 않는다 — `accuracy.PAIRS` 의 나쁜 쪽을 그대로 쓴다.
같은 광고를 두 군데 적어두면 한쪽만 고쳐져 어긋난다.

## 쓰는 법

```bash
MODEL_PROFILE=openai python eval/run_label_check.py            # 1회 (눈대중, 56콜)
MODEL_PROFILE=openai python eval/run_label_check.py --runs 3   # 판정하려면 이쪽 (168콜)
MODEL_PROFILE=openai python eval/run_label_check.py --fixture  # DuckDB 없이
```

**기본은 실경로**(DuckDB → LLM 서사 → 평가)다. 화면이 실제로 타는 길에서 재야
한다. `--fixture` 는 ETL 없이 빠르게 볼 때만 쓴다.

`time_bad`("새벽 감성")는 **일부러 판정하지 않는다.** 시점이 어긋난 것은 맞지만
라벨 다섯 개 중 어디로 가야 하는지 사람도 갈린다. 답이 갈리는 것을 정답으로
박아두면 통과해도 무의미하고 틀려도 원인을 모른다.

## 실측 결과 (2026-08-18, `main` `da5665f`, --runs 3)

```
광고          기대        기대 라벨의 비중   맞은 횟수
price_bad    price              100%        3/3   맞음
target_bad   relevance           14%        0/3   틀림(밀려 있음)
clear_bad    message              0%        0/3   → 판정에서 뺐다 (아래)
```

**1회만 재면 못 믿는다.** 같은 코드로 `target_bad` 를 1회씩 세 번 쟀을 때
`alternative`(틀림) · `relevance`(맞음) · `relevance`(맞음) 로 갈렸는데,
`--runs 3` 으로는 0/3 이었다. 1회 결과로 "고쳤다/망가졌다"를 말하면 안 된다.

`target_bad` 원인 가설: `RESISTANCE_SYSTEM` 의 예시 네 줄에 `relevance` 만 없다.
같은 코멘트에 예시 한 줄만 더해 분류를 다시 돌리면 `alternative` 7 → 0,
`relevance` 1 → 8 로 뒤집힌다. 상세는 `진행보고서/2026-08-18_수호님전달_*.md`.

## 닿지 않는 라벨 — `message` · `none`

`clear_bad` 는 처음에 `message` 를 기대했다가 **판정에서 뺐다.** 지금 구조에서
나올 수 없기 때문이다. 손님 프롬프트가 반응마다 **동네 숫자로 근거를 대라**고
요구하는데, "무슨 말인지 모르겠다"는 상권의 성질이 아니라 **문구의 성질**이라
댈 숫자가 없다.

```
라벨          인용할 동네 숫자          손님이 고르나
price        avg_ticket               예 — clear_bad 에서 12/12
alternative  competitor_cnt           예 — 상품·상황을 가리니 11/12
relevance    age_share · work_ratio   예 — target_bad 에서 9/18
message      없음                     0
none         없음                     0
```

그래서 `relevance` 와 `message` 는 성격이 다르다. `relevance` 는 인용할 숫자가
있으니 분류 예시로 살아나지만 `message` 는 예시를 넣어도 안 나온다. 근거 관문을
손보는 것은 설계 결정이라 담당(수호님)께 넘겼다.

## 알려진 제약

- 상권 1곳(역삼) · **판정은 광고 2건**뿐이다. 다른 상권·업종은 안 봤다.
- `message` · `none` 은 위 이유로 기대값에 못 쓴다. 근거 관문이 바뀌면 다시 넣는다.
- CI 에 넣지 않는다 — 실호출이 나가고, 팀 규칙상 테스트는 mock 만 쓴다.

## 변경 이력

| 버전 | 날짜 | 변경 | 이유 |
|---|---|---|---|
| **V1** | 2026-08-18 | 최초 추가 | 걸림돌 라벨 회귀가 점수 검사를 그대로 통과해 샜다 (relevance 소실) |
